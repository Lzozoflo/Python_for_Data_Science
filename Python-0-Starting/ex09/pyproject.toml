[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "Piscine - Python - 0 - Starting ex09"
readme = "README.md"
requires-python = "<=3.10"